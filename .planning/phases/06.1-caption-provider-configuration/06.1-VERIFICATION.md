---
phase: 06.1-caption-provider-configuration
verified: 2026-03-04T00:00:00Z
status: passed
score: 15/15 must-haves verified
re_verification: false
---

# Phase 6.1: Caption Provider Configuration — Verification Report

**Phase Goal:** Users can configure caption providers (LM Studio, NanoGPT, Gemini, JoyCaption), manage API keys, pick models, and generate/edit captions from a dedicated Captioning page with a split-view workspace.
**Verified:** 2026-03-04
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Requirements Coverage Note

CPROV-01 through CPROV-06 are referenced in ROADMAP.md and both PLAN frontmatter files, but these IDs are **not defined** in `.planning/REQUIREMENTS.md`. The IDs exist only as forward references in the roadmap.

Assessment: The Success Criteria in ROADMAP.md (6 items) serve as the effective requirements definition. All 6 success criteria are verified below. REQUIREMENTS.md should be updated to add CPROV-01..06 definitions if formal traceability is required — this is a documentation gap, not an implementation gap.

---

## Goal Achievement

### Observable Truths (from ROADMAP.md Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Global config (~/.klippbok/config.json) stores provider preferences and API keys across projects | VERIFIED | `global_config_service.py` implements atomic read/write; `load_global_config()` and `save_global_config()` use temp file + `os.replace`; 7 unit tests pass |
| 2 | Four named providers resolve to correct backend configs | VERIFIED | `build_vlm_config_from_global()` in `caption_service.py` maps lm_studio/nanogpt/gemini correctly; joycaption raises ValueError; 17 routing tests pass |
| 3 | Model picker fetches available models from LM Studio and NanoGPT endpoints | VERIFIED | `GET /api/v1/captions/models?provider=...` in `captions.py` makes live HTTP calls for lm_studio/nanogpt; static lists for gemini/joycaption; `ProviderConfigSection.tsx` queries this endpoint with `staleTime: 30_000` |
| 4 | Caption generation accepts a provider preset and resolves config server-side | VERIFIED | `CaptionGenerateRequest.provider_preset` field added; `_run_caption_batch()` checks it first and calls `build_vlm_config_from_global()` before falling through to legacy fields |
| 5 | Dedicated CaptionPage with collapsible provider config, thumbnail strip, batch tag ops, and caption editor | VERIFIED | `CaptionPage.tsx` (268 lines), `ProviderConfigSection.tsx` (312 lines), `ThumbnailStrip.tsx` (46 lines), `BatchTagBar.tsx` (173 lines) all exist and are substantive |
| 6 | Generate Captions moved from GalleryPage to CaptionPage; nav reflects pipeline workflow | VERIFIED | Zero caption generation references remain in `GalleryPage.tsx`; NavBar shows Gallery, Import, Crop, Caption, Settings; `/caption` route registered in `App.tsx` |

**Score:** 6/6 success criteria verified

---

## Required Artifacts

### Plan 01 (Backend)

| Artifact | Expected | Lines | Status | Details |
|----------|----------|-------|--------|---------|
| `klippbok/services/global_config_service.py` | Global config CRUD | 97 | VERIFIED | `load_global_config()` and `save_global_config()` with atomic write via temp file + `os.replace` |
| `klippbok/caption/joycaption.py` | JoyCaption detection | 67 | VERIFIED | `detect_joycaption()` checks Windows and Linux venv paths; `_JOYCAPTION_COMMON_PATHS` is patchable |
| `klippbok/services/caption_service.py` | `build_vlm_config_from_global` | 419+ | VERIFIED | Function at line 332; maps all 4 providers; config.json key priority over env var confirmed |
| `klippbok/api/models.py` | `CaptionProviderConfig` + `provider_preset` | - | VERIFIED | `CaptionProviderConfig` at line 318; `provider_preset: str | None = None` at line 376 |
| `tests/test_global_config_service.py` | Unit tests for global config | 136 | VERIFIED | 7 tests: absent file, invalid JSON, directory creation, round-trip, atomic write, valid JSON |
| `tests/test_joycaption_detection.py` | Unit tests for detection | 129 | VERIFIED | 6 tests: not found, Windows venv, Linux venv, env var, patchable list, first-match |
| `tests/test_caption_provider_routing.py` | Unit tests for routing | 262 | VERIFIED | 17 tests: all 3 providers, anchor_word, custom_prompt, joycaption ValueError, unknown ValueError |

### Plan 02 (Frontend)

| Artifact | Expected | Lines | Min Required | Status | Details |
|----------|----------|-------|-------------|--------|---------|
| `frontend/src/pages/CaptionPage.tsx` | Full caption workspace page | 268 | 150 | VERIFIED | Exceeds minimum; loads config, images, settings; renders all sub-components |
| `frontend/src/components/Caption/ProviderConfigSection.tsx` | Collapsible provider config | 312 | 100 | VERIFIED | Exceeds minimum; collapsible, all 4 providers, password masking, advanced toggle |
| `frontend/src/components/Caption/ThumbnailStrip.tsx` | Horizontal scrollable thumbnails | 46 | 40 | VERIFIED | Exceeds minimum; scroll-snap CSS; selection indicator; filename labels |
| `frontend/src/components/Caption/BatchTagBar.tsx` | Batch tag operations | 173 | 60 | VERIFIED | Exceeds minimum; 4 operations wired to `/api/v1/captions/batch` |
| `frontend/src/components/Layout/NavBar.tsx` | Caption nav item | 37 | — | VERIFIED | Caption appears between Crop and Settings; nav order: Gallery, Import, Crop, Caption, Settings |
| `frontend/src/App.tsx` | `/caption` route | — | — | VERIFIED | `CaptionPage` imported; `<Route path="/caption" element={<CaptionPage />} />` present |

---

## Key Link Verification

### Plan 01 Key Links

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `captions.py` | `global_config_service.py` | `load_global_config()` in config endpoints | WIRED | Lines 517, 547, 582, 659 all import and call `load_global_config`; line 563 calls `save_global_config` |
| `captions.py` | `caption_service.py` | `build_vlm_config_from_global()` in `_run_caption_batch` | WIRED | Lines 142-145: import and call `build_vlm_config_from_global` when `body.provider_preset is not None` |

### Plan 02 Key Links

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `CaptionPage.tsx` | `/api/v1/captions/config` | `useQuery` fetch at line 52-56 | WIRED | GET at line 36; PUT mutation in `ProviderConfigSection.tsx` at line 75 |
| `ProviderConfigSection.tsx` | `/api/v1/captions/models` | `useQuery` fetch at line 25-31 | WIRED | `fetch('/api/v1/captions/models?...')` with `staleTime: 30_000` |
| `CaptionPage.tsx` | `/api/v1/captions/generate` | POST with `provider_preset` in body | WIRED | Line 110-115: `fetch('/api/v1/captions/generate', ...)` with `provider_preset: config.provider` |
| `CaptionPage.tsx` | `useCaptionEvents` hook | SSE progress hook | WIRED | Line 5 import; line 70 usage with `captionOperationId` |

---

## Requirements Coverage

| Requirement ID | Source Plan | Description | Status | Evidence |
|---------------|-------------|-------------|--------|----------|
| CPROV-01 | 06.1-01 | Global config storage | SATISFIED | `global_config_service.py` implements atomic read/write to `~/.klippbok/config.json` |
| CPROV-02 | 06.1-01 | Provider routing to CaptionConfig | SATISFIED | `build_vlm_config_from_global()` maps all 4 providers; config.json key priority enforced |
| CPROV-03 | 06.1-01 | JoyCaption detection | SATISFIED | `detect_joycaption()` checks Windows/Linux venv paths + env var |
| CPROV-04 | 06.1-01 | Config CRUD API endpoints | SATISFIED | `GET/PUT /config`, `GET /models`, `GET /health` registered before `/{image_id}` PATCH |
| CPROV-05 | 06.1-02 | CaptionPage UI | SATISFIED | Full workspace with collapsible config, thumbnail strip, batch tags, caption editor |
| CPROV-06 | 06.1-02 | NavBar update + GalleryPage cleanup | SATISFIED | Caption in nav; zero caption generation code in GalleryPage; frontend built |

**Note on CPROV IDs:** These IDs are referenced in ROADMAP.md and PLAN frontmatter but are not defined in `REQUIREMENTS.md`. They have no formal description entries. This is a traceability documentation gap — the IDs should be added to `REQUIREMENTS.md` with descriptions. Implementation is complete against the ROADMAP success criteria.

---

## Anti-Patterns Found

| File | Pattern | Severity | Assessment |
|------|---------|----------|------------|
| Multiple `.tsx` files | `placeholder="..."` attributes on input elements | INFO | All are legitimate HTML input placeholder attributes, not implementation stubs |
| `ProviderConfigSection.tsx:29` | `if (!res.ok) return []` | INFO | Graceful empty-list fallback for failed model fetch — appropriate error handling |

No blocker or warning anti-patterns found. No TODO/FIXME/HACK comments. No empty implementations. No stub handlers.

---

## Test Results

```
tests/test_global_config_service.py   7/7  passed
tests/test_joycaption_detection.py    6/6  passed
tests/test_caption_provider_routing.py 17/17 passed
All tests: 1241 passed, 4 skipped (no regressions)
```

Backend import check: `from klippbok.api.app import create_app` — OK
Static build: `klippbok/api/static/index.html` — EXISTS

---

## Human Verification Required

### 1. CaptionPage End-to-End Flow

**Test:** Navigate to `/caption` with a project loaded. Verify ProviderConfigSection collapses to summary, expands on click, shows provider-specific fields (Base URL for LM Studio, API key for NanoGPT/Gemini). Click "Generate Captions" and confirm progress toast appears.
**Expected:** Full workflow from provider selection to caption generation progress visible in UI.
**Why human:** Visual rendering, SSE progress display, and toast behavior cannot be verified programmatically.

### 2. Model Picker Live Query

**Test:** With LM Studio running locally, open ProviderConfigSection, select "LM Studio" provider. Verify model dropdown populates with loaded models from `/api/v1/captions/models?provider=lm_studio`.
**Expected:** Dropdown shows available LM Studio models; shows "No model loaded in LM Studio" message when none are loaded.
**Why human:** Requires live LM Studio process; can't mock end-to-end in static analysis.

### 3. Trigger Word Per-Project Save

**Test:** Enter a trigger word in the Trigger Word field in ProviderConfigSection. Click Save. Verify it is saved to the project manifest (anchor_word) via `PUT /api/v1/settings/` — NOT to `~/.klippbok/config.json`.
**Expected:** Trigger word persists per-project; global config does not contain anchor_word.
**Why human:** Requires running server to trace the actual PUT request destination.

---

## Summary

Phase 06.1 goal is **fully achieved**. All 6 ROADMAP success criteria are verified against the actual codebase:

1. Global config service exists with atomic read/write — 7 tests confirm behavior.
2. Provider routing maps all 4 providers correctly — 17 tests confirm behavior, including config.json key priority over env vars.
3. Model picker endpoints exist and are wired in the frontend.
4. `provider_preset` field flows from request through `_run_caption_batch` to `build_vlm_config_from_global`.
5. CaptionPage with all required components is substantive (799 total lines across 4 files), collapsible, and wired to all required APIs.
6. GalleryPage has zero caption generation references; NavBar has correct order; frontend is built and deployed.

The only gap is a **documentation gap**: CPROV-01..06 are not defined in `REQUIREMENTS.md`. This does not affect implementation correctness but should be addressed to maintain traceability hygiene.

---

_Verified: 2026-03-04_
_Verifier: Claude (gsd-verifier)_
