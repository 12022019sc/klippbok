---
phase: 04-web-gui-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/vite.config.ts
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/App.css
  - frontend/src/index.css
  - frontend/src/pages/GalleryPage.tsx
  - frontend/src/pages/ImportPage.tsx
  - frontend/src/pages/SettingsPage.tsx
  - frontend/src/components/Layout/NavBar.tsx
  - frontend/src/components/Layout/AppLayout.tsx
  - frontend/src/stores/appStore.ts
autonomous: true

must_haves:
  truths:
    - "npm run dev starts Vite dev server with HMR"
    - "Browser shows app shell with top navigation bar"
    - "Clicking Gallery, Import, Settings links navigates between pages"
    - "API calls from frontend are proxied to localhost:8000 during development"
  artifacts:
    - path: "frontend/package.json"
      provides: "React + Vite project with all required dependencies"
      contains: "masonic"
    - path: "frontend/src/App.tsx"
      provides: "React Router configuration with 3 routes"
      contains: "react-router"
    - path: "frontend/src/components/Layout/NavBar.tsx"
      provides: "Top navigation bar with links to Gallery, Import, Settings"
    - path: "frontend/src/stores/appStore.ts"
      provides: "Zustand store for global UI state"
      exports: ["useAppStore"]
  key_links:
    - from: "frontend/vite.config.ts"
      to: "http://localhost:8000"
      via: "proxy config for /api"
      pattern: "proxy.*api"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/*"
      via: "react-router Route elements"
      pattern: "Route.*path"
---

<objective>
Scaffold the React SPA frontend with Vite, TypeScript, React Router, zustand, TanStack Query, and app shell layout.

Purpose: Provides the frontend foundation that gallery (Plan 03) and import/settings pages (Plan 04) build on. Establishes routing, state management, API data fetching patterns, and the navigation shell.

Output: Working Vite dev server with nav bar, three page stubs (Gallery, Import, Settings), zustand store, TanStack Query provider, and Vite proxy for API calls.
</objective>

<execution_context>
@C:\Users\Shayne\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Shayne\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-web-gui-foundation/04-RESEARCH.md
@.planning/phases/04-web-gui-foundation/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React + TypeScript project with dependencies</name>
  <files>
    frontend/package.json
    frontend/tsconfig.json
    frontend/tsconfig.app.json
    frontend/vite.config.ts
    frontend/index.html
    frontend/src/main.tsx
    frontend/src/vite-env.d.ts
  </files>
  <action>
    Create the frontend project. Run from project root:
    ```
    npm create vite@latest frontend -- --template react-ts
    ```

    Then install dependencies:
    ```
    cd frontend && npm install react-router @tanstack/react-query zustand masonic yet-another-react-lightbox sonner
    ```

    After scaffold is created, update **vite.config.ts** to add API proxy:
    ```typescript
    import { defineConfig } from 'vite'
    import react from '@vitejs/plugin-react'

    export default defineConfig({
      plugins: [react()],
      server: {
        proxy: {
          '/api': 'http://localhost:8000',
        },
      },
    })
    ```

    This ensures all `/api/*` requests from the Vite dev server are proxied to the FastAPI backend, avoiding CORS issues during development.
  </action>
  <verify>
    Run: `cd frontend && npm run build` -- should compile without errors.
  </verify>
  <done>
    Vite + React + TypeScript scaffold exists at `frontend/` with all required npm dependencies installed and proxy configured.
  </done>
</task>

<task type="auto">
  <name>Task 2: App shell with NavBar, routing, zustand store, and query provider</name>
  <files>
    frontend/src/main.tsx
    frontend/src/App.tsx
    frontend/src/App.css
    frontend/src/index.css
    frontend/src/pages/GalleryPage.tsx
    frontend/src/pages/ImportPage.tsx
    frontend/src/pages/SettingsPage.tsx
    frontend/src/components/Layout/NavBar.tsx
    frontend/src/components/Layout/AppLayout.tsx
    frontend/src/stores/appStore.ts
  </files>
  <action>
    **frontend/src/stores/appStore.ts**: Zustand store with:
    - `importOperationId: string | null` -- active import operation ID (for SSE tracking)
    - `importProgress: { current: number; total: number; message: string } | null` -- import progress state
    - `setImportOperationId(id: string | null)` -- setter
    - `setImportProgress(progress)` -- setter
    - `clearImport()` -- resets both to null
    Use `create` from zustand. Export `useAppStore`.

    **frontend/src/components/Layout/NavBar.tsx**: Top navigation bar component:
    - Horizontal bar with app name "klippbok" on the left
    - Navigation links: Gallery, Import, Settings (use `NavLink` from react-router for active state styling)
    - Style with plain CSS (in App.css): dark background (#1a1a2e or similar dark theme), white text, active link underlined or highlighted
    - Use semantic HTML: `<nav>` with `<ul>` of links

    **frontend/src/components/Layout/AppLayout.tsx**: Layout wrapper:
    - Renders NavBar at top
    - Renders `<Outlet />` from react-router for page content below
    - Wraps content in a container div with padding

    **frontend/src/App.tsx**: React Router setup:
    - Import `BrowserRouter`, `Routes`, `Route` from `react-router` (NOT react-router-dom -- v7 merged)
    - Root route uses AppLayout as the layout element
    - Three child routes: "/" -> GalleryPage, "/import" -> ImportPage, "/settings" -> SettingsPage
    - Default redirect: "/" is Gallery

    **frontend/src/main.tsx**: Entry point:
    - Wrap App in `QueryClientProvider` from @tanstack/react-query with a new QueryClient
    - Render into `#root`
    - Import App.css and index.css

    **frontend/src/index.css**: Global styles:
    - Dark theme defaults: `body { margin: 0; background: #0f0f1a; color: #e0e0e0; font-family: system-ui, sans-serif; }`
    - Box-sizing border-box reset
    - Remove Vite default styles

    **frontend/src/App.css**: App-level styles:
    - Nav bar: `.navbar { background: #1a1a2e; padding: 0 1rem; display: flex; align-items: center; height: 48px; gap: 2rem; }`
    - Nav links: `.nav-link { color: #9ca3af; text-decoration: none; } .nav-link.active { color: #fff; border-bottom: 2px solid #6366f1; }`
    - App name: `.app-name { font-weight: 700; color: #fff; font-size: 1.1rem; }`
    - Page container: `.page-container { padding: 1.5rem; max-width: 1400px; margin: 0 auto; }`

    **Page stubs** (GalleryPage, ImportPage, SettingsPage): Simple placeholder components with page title heading. GalleryPage shows "Gallery" heading with a note "Gallery will be implemented in Plan 03". ImportPage shows "Import" heading. SettingsPage shows "Settings" heading. Each is a default export.
  </action>
  <verify>
    Run: `cd frontend && npm run build` -- compiles without TypeScript errors.
    Run: `cd frontend && npx tsc --noEmit` -- type checks pass.
  </verify>
  <done>
    App shell renders with dark-themed nav bar linking to Gallery, Import, Settings pages. Zustand store exists with import progress state. TanStack Query provider wraps the app. All three routes navigate correctly.
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` -- builds without errors
2. `cd frontend && npx tsc --noEmit` -- no TypeScript errors
3. `frontend/node_modules/masonic` exists -- masonic installed
4. `frontend/node_modules/yet-another-react-lightbox` exists -- lightbox installed
5. `frontend/node_modules/sonner` exists -- sonner installed
</verification>

<success_criteria>
- Vite dev server starts with `npm run dev` and serves the app shell
- NavBar shows app name and three navigation links
- React Router navigates between Gallery, Import, Settings pages
- Vite proxy forwards /api/* to localhost:8000
- Zustand store manages import operation state
- TanStack Query provider wraps the application
</success_criteria>

<output>
After completion, create `.planning/phases/04-web-gui-foundation/04-02-SUMMARY.md`
</output>
