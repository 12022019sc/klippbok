---
phase: 04-web-gui-foundation
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - frontend/src/pages/GalleryPage.tsx
  - frontend/src/components/Gallery/MasonryGrid.tsx
  - frontend/src/components/Gallery/ThumbnailCard.tsx
  - frontend/src/components/Gallery/StatusStrip.tsx
  - frontend/src/components/Lightbox/ImageLightbox.tsx
  - frontend/src/hooks/useImages.ts
  - frontend/src/types/image.ts
  - frontend/src/utils/duplicateColors.ts
  - frontend/src/App.css
autonomous: true

must_haves:
  truths:
    - "Gallery page shows a masonry grid of image thumbnails loaded from API"
    - "Each thumbnail has a semi-transparent bottom strip showing resolution, quality, and bucket"
    - "Near-duplicate images have matching colored borders"
    - "Clicking a thumbnail opens a lightbox with full-size image and metadata panel"
    - "Gallery virtualizes rendering (only visible items in DOM)"
  artifacts:
    - path: "frontend/src/components/Gallery/MasonryGrid.tsx"
      provides: "Virtualized masonry grid using masonic library"
      contains: "Masonry"
    - path: "frontend/src/components/Gallery/ThumbnailCard.tsx"
      provides: "Single thumbnail card with status overlay and duplicate border"
    - path: "frontend/src/components/Gallery/StatusStrip.tsx"
      provides: "Semi-transparent bottom overlay with resolution, quality, bucket indicators"
    - path: "frontend/src/components/Lightbox/ImageLightbox.tsx"
      provides: "Lightbox overlay with metadata panel via slideFooter"
      contains: "yet-another-react-lightbox"
    - path: "frontend/src/hooks/useImages.ts"
      provides: "TanStack Query hook fetching GET /api/v1/images"
      exports: ["useImages"]
    - path: "frontend/src/types/image.ts"
      provides: "TypeScript interfaces matching API response shapes"
      exports: ["GalleryItem", "GalleryResponse"]
  key_links:
    - from: "frontend/src/hooks/useImages.ts"
      to: "/api/v1/images"
      via: "fetch call with TanStack Query"
      pattern: "fetch.*api/v1/images"
    - from: "frontend/src/pages/GalleryPage.tsx"
      to: "frontend/src/components/Gallery/MasonryGrid.tsx"
      via: "renders MasonryGrid with fetched items"
      pattern: "MasonryGrid"
    - from: "frontend/src/components/Gallery/ThumbnailCard.tsx"
      to: "frontend/src/components/Lightbox/ImageLightbox.tsx"
      via: "onClick opens lightbox"
---

<objective>
Build the image gallery page with virtualized masonry layout, per-image status overlays, duplicate grouping visuals, and click-to-expand lightbox.

Purpose: The gallery is the primary view users see. It must handle large datasets (1000+ images) with virtualized rendering, show status at a glance via overlay strips, visually group duplicates, and allow detailed inspection via lightbox.

Output: Fully functional gallery page fetching from the images API, rendering thumbnails in masonry layout with status indicators, and opening a lightbox with metadata on click.
</objective>

<execution_context>
@C:\Users\Shayne\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Shayne\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-web-gui-foundation/04-RESEARCH.md
@.planning/phases/04-web-gui-foundation/04-CONTEXT.md
@.planning/phases/04-web-gui-foundation/04-01-SUMMARY.md
@.planning/phases/04-web-gui-foundation/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript types, useImages hook, and duplicate color utility</name>
  <files>
    frontend/src/types/image.ts
    frontend/src/hooks/useImages.ts
    frontend/src/utils/duplicateColors.ts
  </files>
  <action>
    **frontend/src/types/image.ts**: TypeScript interfaces matching API response:
    ```typescript
    export interface GalleryItem {
      id: string;
      relative_path: string;
      thumbnail_url: string;
      width: number;
      height: number;
      resolution_ok: boolean;
      quality_pass: boolean;
      bucket: string | null;
      is_near_duplicate: boolean;
      duplicate_group_id: string | null;
      caption: string | null;
    }

    export interface GalleryResponse {
      total: number;
      images: GalleryItem[];
    }
    ```

    **frontend/src/hooks/useImages.ts**: TanStack Query hook:
    - `useImages()`: Uses `useQuery` with queryKey `["images"]`. Fetches `GET /api/v1/images`. Returns `{ data, isLoading, error, refetch }`. The fetch function parses JSON as `GalleryResponse`. On error, throw with message.

    **frontend/src/utils/duplicateColors.ts**: Stable color assignment for duplicate groups:
    - `DUPLICATE_COLORS` array: 7 distinct colors (red, orange, yellow, green, cyan, purple, pink).
    - `getDuplicateGroupColor(groupId: string): string` -- uses a Map cache to assign stable color per group ID. Cycles through DUPLICATE_COLORS by insertion order.
    - Export both the function and the cache (for testing).
  </action>
  <verify>
    Run: `cd frontend && npx tsc --noEmit` -- no TypeScript errors.
  </verify>
  <done>
    GalleryItem and GalleryResponse types exist. useImages hook fetches from /api/v1/images via TanStack Query. Duplicate color utility assigns stable colors per group.
  </done>
</task>

<task type="auto">
  <name>Task 2: Masonry grid, thumbnail cards with status strip, and lightbox</name>
  <files>
    frontend/src/components/Gallery/MasonryGrid.tsx
    frontend/src/components/Gallery/ThumbnailCard.tsx
    frontend/src/components/Gallery/StatusStrip.tsx
    frontend/src/components/Lightbox/ImageLightbox.tsx
    frontend/src/pages/GalleryPage.tsx
    frontend/src/App.css
  </files>
  <action>
    **frontend/src/components/Gallery/StatusStrip.tsx**: Bottom overlay bar:
    - Props: `resolution_ok: boolean`, `quality_pass: boolean`, `bucket: string | null`, `width: number`, `height: number`.
    - Renders a semi-transparent dark bar (`background: rgba(0,0,0,0.7)`) pinned to bottom of card.
    - Shows: resolution badge (e.g. "1920x1080") with green (#22c55e) or red (#ef4444) dot based on resolution_ok. Quality badge: "Sharp" in green or "Blur" in red based on quality_pass. Bucket label if present (e.g. "3:4") in neutral gray.
    - Font size small (~11px). Flex layout with gap. Truncate if too wide.

    **frontend/src/components/Gallery/ThumbnailCard.tsx**: Single thumbnail card:
    - Props: `item: GalleryItem`, `width: number` (from masonic), `onClick: () => void`.
    - Calculate height from aspect ratio: `Math.round(width * (item.height / item.width))`.
    - Render a container div with explicit width and calculated height.
    - Inside: `<img>` with `src={item.thumbnail_url}` loading="lazy", width/height set.
    - Overlay: StatusStrip at bottom.
    - If `item.is_near_duplicate && item.duplicate_group_id`: Apply a 3px colored left border using `getDuplicateGroupColor(item.duplicate_group_id)`. This makes duplicate clusters visually obvious in the masonry grid.
    - On click: call onClick prop.
    - CSS: `position: relative; overflow: hidden; border-radius: 4px; cursor: pointer;` with hover effect (slight brightness increase).

    **frontend/src/components/Gallery/MasonryGrid.tsx**: Virtualized masonry:
    - Props: `items: GalleryItem[]`, `onItemClick: (item: GalleryItem, index: number) => void`.
    - Uses `Masonry` from masonic with `columnGutter={8}`, `columnWidth={220}`, `overscanBy={2}`.
    - The render function receives `{ index, data, width }` and renders `ThumbnailCard` with calculated height, passing onClick.
    - The items array is passed directly to masonic.

    **frontend/src/components/Lightbox/ImageLightbox.tsx**: Click-to-expand lightbox:
    - Props: `items: GalleryItem[]`, `currentIndex: number`, `open: boolean`, `onClose: () => void`.
    - Uses `Lightbox` from yet-another-react-lightbox.
    - Import the lightbox CSS: `import "yet-another-react-lightbox/styles.css"`.
    - Map GalleryItem[] to slides: `{ src: "/api/v1/images/{id}/full" }` -- WAIT, we don't have a full image endpoint yet. For Phase 4, use the thumbnail URL as the lightbox source too (or serve the original file). Use: `src: item.thumbnail_url` for now with a TODO comment noting Phase 5 will add a full-resolution endpoint. Actually, serve the original image directly: construct src as `/api/v1/images/${item.id}/original`. We need to add this endpoint -- add a NOTE in the code that Plan 01's images router should be updated to serve originals too. For now, fall back to thumbnail_url.
    - CORRECTION: Use thumbnail_url for lightbox slides in Phase 4. The full-resolution endpoint is not critical for MVP and can be added when the crop editor needs it (Phase 5).
    - Use `render.slideFooter` for metadata panel: Show relative_path, resolution (width x height), bucket, quality status, caption (if present), duplicate status.
    - Style the footer with dark semi-transparent background, padding, readable text.

    **frontend/src/pages/GalleryPage.tsx**: Replace stub with full gallery:
    - Call `useImages()` hook.
    - Show loading spinner/text while loading.
    - Show error message if fetch fails.
    - Show "No images imported yet" if data.images is empty.
    - Otherwise render MasonryGrid with items.
    - Track lightbox state: `selectedIndex: number | null`. When item clicked, set selectedIndex. Render ImageLightbox when selectedIndex !== null.

    **frontend/src/App.css**: Add gallery-specific styles:
    - `.gallery-container { width: 100%; }` -- full-width for masonry
    - `.thumbnail-card { position: relative; overflow: hidden; border-radius: 4px; cursor: pointer; transition: filter 0.2s; }`
    - `.thumbnail-card:hover { filter: brightness(1.1); }`
    - `.thumbnail-card img { display: block; width: 100%; height: 100%; object-fit: cover; }`
    - `.status-strip { position: absolute; bottom: 0; left: 0; right: 0; background: rgba(0,0,0,0.7); padding: 4px 6px; display: flex; align-items: center; gap: 6px; font-size: 11px; color: #e0e0e0; }`
    - `.status-dot { width: 6px; height: 6px; border-radius: 50%; display: inline-block; }`
    - `.status-dot.ok { background: #22c55e; }`
    - `.status-dot.fail { background: #ef4444; }`
    - `.lightbox-footer { background: rgba(0,0,0,0.8); padding: 12px 16px; color: #e0e0e0; font-size: 13px; display: flex; gap: 16px; flex-wrap: wrap; }`
    - `.duplicate-border { border-left-width: 3px; border-left-style: solid; }`
  </action>
  <verify>
    Run: `cd frontend && npm run build` -- compiles without errors.
    Run: `cd frontend && npx tsc --noEmit` -- type checks pass.
  </verify>
  <done>
    Gallery page renders masonry grid from API data. Each thumbnail shows status strip with resolution, quality, and bucket indicators. Duplicate images have colored left borders. Clicking opens lightbox with metadata footer.
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` -- production build succeeds
2. `cd frontend && npx tsc --noEmit` -- no type errors
3. GalleryPage imports and renders MasonryGrid, ThumbnailCard, StatusStrip, ImageLightbox
4. useImages hook fetches from /api/v1/images
5. StatusStrip shows green/red indicators
6. ThumbnailCard applies duplicate border color
</verification>

<success_criteria>
- Gallery page fetches images from API and renders in virtualized masonry grid
- Each thumbnail shows semi-transparent status strip with resolution (green/red dot), quality (sharp/blur), and bucket label
- Near-duplicate images have matching colored left borders
- Clicking a thumbnail opens a lightbox with image and metadata footer
- Empty state shows "No images imported yet" message
- Loading state shows indicator while fetching
</success_criteria>

<output>
After completion, create `.planning/phases/04-web-gui-foundation/04-03-SUMMARY.md`
</output>
