---
phase: 06.2-captioning-enhancement-joycaption-integration-planning
verified: 2026-03-05T07:00:00Z
status: passed
score: 6/6 success criteria verified
re_verification:
  previous_status: gaps_found
  previous_score: 5/6
  gaps_closed:
    - "Post-processing pipelines produce clean output (artifact stripping, token trimming) — apply_vlm_pipeline step order fixed; new regression tests added and passing"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Verify mode dropdown, max-tokens field, and over-budget warning appear in CaptionPage"
    expected: "Mode dropdown (5 options, default=Context Only Tags), Max Tokens number field (placeholder 'Auto'), over-budget warning (orange text) visible when caption exceeds budget"
    why_human: "Frontend visual verification — requires browser inspection"
  - test: "Verify config round-trip: set mode=Descriptive + max_tokens=100, save, reload page, confirm persistence"
    expected: "Values persist across page reload (stored in config.json via PUT /captions/config)"
    why_human: "State persistence requires live server"
---

# Phase 6.2: Captioning Enhancement — JoyCaption Integration Verification Report

**Phase Goal:** Enhance the captioning system with JoyCaption-inspired caption modes, context-only modes for character LoRA training, a multi-stage post-processing pipeline, model-aware token budgets, and trigger word/subject tag injection — applying to all providers and models
**Verified:** 2026-03-05T07:00:00Z
**Status:** passed
**Re-verification:** Yes — after gap closure (Plan 04)

## Goal Achievement

### Observable Truths (from ROADMAP.md Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | 5 caption modes available across all providers | VERIFIED | `CaptionMode` Literal in `models.py`, 5 prompt templates in `prompts.py`, mode routing in `caption_service.py` and `captions.py` |
| 2 | Context Only modes filter appearance tags | VERIFIED | `_filter_appearance_tags` in `pipeline.py` with `DEFAULT_APPEARANCE_BLACKLIST` (52 entries), applied in `apply_joycaption_pipeline` and `caption_image_for_project` for WD Tagger path |
| 3 | Post-processing pipelines produce clean output (artifact stripping, token trimming) | VERIFIED | `apply_vlm_pipeline` step order fixed (commit `108a505`): `_strip_vlm_artifacts` runs at line 417 BEFORE `_prepend_anchor` at line 419; `TestVlmPipelineAnchorArtifact` class with 2 new regression tests; spot-check `apply_vlm_pipeline("Here is a caption: a girl in the park", anchor_word="Luna", ...)` returns `"Luna, a girl in the park"` |
| 4 | Model-aware token budgets enforced with smart trimming | VERIFIED | `ModelProfile.default_token_budget` (SD15=75, SDXL=150, Flux=225, Pony=75); `_trim_to_budget` drops tags from end for tag modes, sentence-boundary trims for NL modes; anchor word protected |
| 5 | Mode dropdown and max-tokens field visible in CaptionPage config section | VERIFIED (automated) | `CAPTION_MODES` constant and dropdown JSX in `ProviderConfigSection.tsx`; `max_tokens` input with placeholder "Auto (from model profile)" |
| 6 | Over-budget warning indicator visible per image | VERIFIED (automated) | `isOverBudget()` in `CaptionPage.tsx`, `<span className="caption-over-budget">` with token count, `.caption-over-budget` CSS in `index.css` |

**Score:** 6/6 truths verified

### Required Artifacts

**Plan 01 Artifacts:**

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `klippbok/caption/pipeline.py` | Post-processing pipelines | VERIFIED | `apply_vlm_pipeline`, `apply_joycaption_pipeline`, `DEFAULT_APPEARANCE_BLACKLIST` exported. 474 lines. Step order corrected in Plan 04. |
| `klippbok/caption/models.py` | CaptionMode type, updated CaptionConfig | VERIFIED | `CaptionMode` Literal with 5 values, `CaptionConfig.caption_mode` + `max_tokens`, `use_case` removed with `extra="forbid"` |
| `klippbok/caption/prompts.py` | 5 caption mode prompt templates | VERIFIED | `IMAGE_PROMPTS` dict with 5 keys, `VIDEO_PROMPTS` dict with 5 keys, `get_image_prompt(caption_mode=...)` |
| `klippbok/config/model_profiles.py` | ModelProfile with default_token_budget | VERIFIED | `default_token_budget: int = Field(default=150, ...)` present |
| `tests/test_caption_pipeline.py` | Pipeline tests | VERIFIED | 78 tests (76 original + 2 regression tests from Plan 04), all passing |

**Plan 02 Artifacts:**

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `klippbok/services/caption_service.py` | Mode-aware caption routing | VERIFIED | `get_caption_mode_for_project`, `_resolve_token_budget`, `caption_image_for_project` with mode routing + `apply_vlm_pipeline` |
| `klippbok/caption/joycaption.py` | Mode-parameterized JoyCaption runner | VERIFIED | `--mode` arg in `argparse`, `MODE_PROMPTS` dict with 5 mode prompts, `run_joycaption_image(caption_mode=...)` |
| `klippbok/api/routers/captions.py` | Updated caption generation endpoint | VERIFIED | `effective_caption_mode` used throughout, no `body.style` references |
| `klippbok/api/models.py` | `CaptionProviderConfig` with `appearance_blacklist_extra` | VERIFIED | `appearance_blacklist_extra: list[str] = []` at line 364 |

**Plan 03 Artifacts:**

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `frontend/src/components/Caption/ProviderConfigSection.tsx` | Mode dropdown + max-tokens field | VERIFIED | `CAPTION_MODES` constant, mode `<select>`, `max_tokens` `<input type="number">` both present |
| `frontend/src/pages/CaptionPage.tsx` | Over-budget warning + caption_mode in generate request | VERIFIED | `isOverBudget()` function, `caption-over-budget` span, generate request sends `caption_mode: config.caption_mode` |

**Plan 04 Artifacts (Gap Closure):**

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `klippbok/caption/pipeline.py` | `apply_vlm_pipeline` with corrected step order | VERIFIED | `_strip_vlm_artifacts` at line 417 (step 1), `_prepend_anchor` at lines 419-420 (step 2); docstring updated to reflect correct order |
| `tests/test_caption_pipeline.py` | `TestVlmPipelineAnchorArtifact` regression class | VERIFIED | Class exists at line 691 with `test_strips_artifacts_with_anchor_word` and `test_strips_artifacts_with_anchor_booru_mode`; both pass |

### Key Link Verification

**Plan 04 Key Link (previously gap):**

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `apply_vlm_pipeline` | `_strip_vlm_artifacts` | called as step 1 before `_prepend_anchor` | VERIFIED | Line 417: `caption = _strip_vlm_artifacts(caption)` precedes lines 419-420 anchor injection. Spot-check confirmed: `"Here is a caption: a girl in the park"` with `anchor_word="Luna"` returns `"Luna, a girl in the park"` |

All previously verified key links from Plans 01-03 remain wired. Full test suite: 1384 passed, 4 skipped, 0 failures — no regressions.

### Requirements Coverage

The CENH-01 through CENH-10 requirement IDs are referenced in ROADMAP.md and plan frontmatter but are not defined in `.planning/REQUIREMENTS.md` (which covers v1 requirements: CAPT, IMG, CROP, MODL, GUI, EXPT, ARCH domains, last updated 2026-02-27 before Phase 6.2 was inserted). These are phase-specific extension requirements defined within the roadmap entry and plan frontmatter. Coverage is assessed against the ROADMAP.md Success Criteria and plan must_haves.

| Requirement | Source Plan | Description (inferred from plan must_haves) | Status |
|-------------|------------|---------------------------------------------|--------|
| CENH-01 | Plan 01 | `CaptionMode` Literal with 5 values | SATISFIED |
| CENH-02 | Plan 01 | `CaptionConfig` updated: `use_case` removed, `caption_mode` + `max_tokens` added | SATISFIED |
| CENH-03 | Plan 01 | `prompts.py` rewritten with 5 mode templates | SATISFIED |
| CENH-04 | Plan 01 | `ModelProfile.default_token_budget` + correct values per profile | SATISFIED |
| CENH-05 | Plan 01 | Post-processing pipelines in `pipeline.py` | SATISFIED — step order fixed in Plan 04 |
| CENH-06 | Plan 02 | `caption_service.py` mode-aware routing | SATISFIED |
| CENH-07 | Plan 02 | JoyCaption runner parameterized with `--mode` | SATISFIED |
| CENH-08 | Plan 02 | `appearance_blacklist_extra` + pipeline integration in router | SATISFIED |
| CENH-09 | Plan 03 | Frontend mode dropdown + max-tokens field | SATISFIED |
| CENH-10 | Plan 03 | Over-budget warning indicator | SATISFIED |

All 10 CENH requirements satisfied.

### Anti-Patterns Found

None. The previously identified pipeline step-order bug (warning-level) is resolved:

- `klippbok/caption/pipeline.py`: `_strip_vlm_artifacts` now runs before `_prepend_anchor` — preamble stripping works on raw VLM output at position 0.
- `tests/test_caption_pipeline.py`: Anchor + preamble combination is now covered by `TestVlmPipelineAnchorArtifact`.

No placeholder components, empty returns, TODO/FIXME patterns, or stubs found in any implementation files.

### Human Verification Required

These items require a running browser session. They were flagged in the initial verification and remain outstanding as informational — they do not block automated verification.

#### 1. CaptionPage Mode Dropdown Visual Verification

**Test:** Start server (`python -m klippbok.api --port 9000`), navigate to Caption page, expand the provider config section
**Expected:** "Caption Mode" dropdown appears with 5 options (Context Only Tags, Context Only Natural, Booru Tags, Descriptive, Straightforward); "Max Tokens" number input with "Auto (from model profile)" placeholder visible
**Why human:** Visual layout verification requires browser

#### 2. Config Persistence Round-Trip

**Test:** Change mode to "Descriptive", set max tokens to 100, click Save; reload page
**Expected:** Mode remains "Descriptive" and max tokens shows 100 after reload (persisted in config.json via PUT /captions/config)
**Why human:** Requires live server + config.json inspection

#### 3. Over-Budget Warning Rendering

**Test:** With max_tokens=null (default 150), find or create an image with a long caption (>115 words approx), view it in caption editor
**Expected:** Orange "(over budget (~X tokens > 150))" text appears above the textarea
**Why human:** Requires real data with long captions in the editor view

### Re-verification Summary

The single gap from the initial verification is closed.

Plan 04 (commit `108a505`) fixed `apply_vlm_pipeline` by swapping the step order: artifact stripping (`_strip_vlm_artifacts`) now runs on raw VLM output before anchor injection (`_prepend_anchor`). The `^`-anchored regex patterns in `_VLM_PREFIX_PATTERNS` now match correctly at position 0 before any anchor text is prepended.

Two regression tests in `TestVlmPipelineAnchorArtifact` cover the previously broken combination:
- Descriptive mode: `"Here is a caption: a girl in the park"` + `anchor_word="Luna"` returns `"Luna, a girl in the park"` (preamble stripped, anchor injected)
- Booru tags mode: `"Caption: smiling, park, standing"` + `anchor_word="Luna"` returns `"Luna, smiling, park, standing"`

Full test suite: 1384 passed, 4 skipped, 0 failures. No regressions in any of the 4 plans' implementations.

All 10 CENH requirements are satisfied. Phase 6.2 automated verification is complete.

---

_Verified: 2026-03-05T07:00:00Z_
_Verifier: Claude (gsd-verifier)_
